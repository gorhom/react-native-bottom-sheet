import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    left: 12,
    top: 12,
    right: 12,
    padding: 6,
    backgroundColor: 'black',
  },
  text: {
    padding: 0,
    fontSize: 20,
    lineHeight: 24,
    color: 'white',
  },
});
